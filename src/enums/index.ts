export enum Action {
    READ = 'read',
    WRITE = 'write',
    UPDATE = 'update',
    DELETE = 'delete'
}

export enum Resource {
    POST = 'post',
}

export enum Role {
    ADMIN = 'ADMIN',
    USER = 'USER',
    EDITOR = 'EDITOR'
}